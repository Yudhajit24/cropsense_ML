# ─── Dataset images (too large for git — download via Kaggle CLI) ──────────────
cropsense/backend/data/soil_images/
cropsense/backend/data/*.zip

# ─── Python environments ────────────────────────────────────────────────────────
cropsense/backend/venv/
cropsense/backend/venv_tf/
**/__pycache__/
**/*.pyc
**/*.pyo
.env

# ─── Trained ML model files (large binaries) ───────────────────────────────────
# Pickle files (regenerated by train.py — commit only if needed for demo)
# cropsense/backend/models/saved/*.pkl  ← uncomment to exclude pickles too

# Exclude the heavy CNN weights but keep the lightweight JSON/PNG outputs 
cropsense/backend/models/saved/soil_cnn.h5

# ─── Frontend build output ──────────────────────────────────────────────────────
cropsense/frontend/node_modules/
cropsense/frontend/dist/
cropsense/frontend/dist-ssr/

# ─── OS / Editor files ──────────────────────────────────────────────────────────
.DS_Store
*.suo
*.sw?
.idea/
.vscode/
*.log
